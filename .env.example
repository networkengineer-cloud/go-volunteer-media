# Application Configuration
ENV=development
PORT=8080

# Logging Configuration
# LOG_LEVEL: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=INFO
# LOG_FORMAT: json or text (default: json)
LOG_FORMAT=json

# JWT Secret (REQUIRED - must be at least 32 characters for security)
# Generate with: openssl rand -base64 32
# NEVER use default values in production!
JWT_SECRET=change-this-to-a-secure-random-32-char-string-in-production

# HSTS Configuration (Enable in production with HTTPS)
# ENABLE_HSTS=true

# CORS Configuration
# Comma-separated list of allowed origins, or "*" for all (not recommended for production)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Database Configuration - Development
# SECURITY: Use strong passwords in production and enable SSL with verify-full
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=volunteer_media_dev
DB_SSLMODE=disable  # Use "require" or "verify-full" in production

# Database Logging Configuration
# Control GORM log verbosity: silent, error, warn, info (default: warn)
DB_LOG_LEVEL=warn

# Email Configuration
# Set EMAIL_ENABLED=false to disable email (useful for dev/test environments)
# EMAIL_ENABLED=true  # Default: enabled (omit or set to anything but "false" or "0")

# Choose email provider: "smtp" or "resend" (default: smtp for backwards compatibility)
EMAIL_PROVIDER=resend

# Resend Configuration (recommended - modern email API)
# Get your API key from https://resend.com
# IMPORTANT: Use your own verified domain in RESEND_FROM_EMAIL (e.g., notifications.myhaws.org)
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=noreply@notifications.myhaws.org
RESEND_FROM_NAME=Haws Volunteers
# RESEND_API_URL=https://api.resend.com/emails  # Optional: Override for testing

# SMTP Configuration (legacy - fallback option)
# Leave empty to disable email features when using Resend
# IMPORTANT: SMTP_FROM_EMAIL can be any domain you control
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@notifications.myhaws.org
SMTP_FROM_NAME=Haws Volunteers

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:5173

# Storage Configuration
# STORAGE_PROVIDER: "postgres" (default, backward compatible) or "azure" (recommended for production)
# Feature flag to enable Azure Blob Storage for images and documents
STORAGE_PROVIDER=azure

# Azure Blob Storage Configuration (required when STORAGE_PROVIDER=azure)
# Create an Azure Storage Account and container before enabling
# Get credentials from Azure Portal -> Storage Account -> Access Keys
AZURE_STORAGE_ACCOUNT_NAME=yourstorageaccount
AZURE_STORAGE_ACCOUNT_KEY=your-base64-encoded-storage-account-key
AZURE_STORAGE_CONTAINER_NAME=uploads
# AZURE_STORAGE_ENDPOINT=  # Optional: custom endpoint (e.g., http://127.0.0.1:10000/devstoreaccount1 for Azurite local testing)

# Azure Storage Authentication (recommended for production: use Managed Identity)
# AZURE_STORAGE_USE_MANAGED_IDENTITY=false  # Set to "true" to use Azure Managed Identity instead of account key

